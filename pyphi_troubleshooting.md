# PyPhi実験でよくある問題と解決方法

## はじめに

このドキュメントは、PyPhiを使用した統合情報理論（IIT）の実験中に発生する可能性のある問題とその解決方法をまとめたものです。

## よくある問題

### 1. 状態の指定に関するエラー

```
Invalid state: there must be one entry per node in the network
```

**問題の説明**：
- PyPhiでは、システムの状態を指定する際に、全てのノードの状態を指定する必要があります
- 例えば2ノードシステムの場合、(1,) ではなく (1, 0) のように2つの値を指定する必要があります

**解決方法**：
1. ネットワーク内の全ノードの数を確認する
2. 状態を指定する際は、必ず全ノードの状態を含める
3. 例：2ノードシステムで最初のノードだけを調べたい場合でも、両方のノードの状態を指定する

### 2. TPMとの互換性エラー

```
The state cannot be reached in the given TPM
```

**問題の説明**：
- 指定した状態が、システムの遷移確率行列（TPM）で定義された状態と互換性がない
- つまり、システムがその状態に到達できない設定になっている

**解決方法**：
1. TPMの定義を確認する
2. 指定している状態がTPMで定義された状態の中に含まれているか確認する
3. 必要に応じてTPMを修正するか、別の状態を選択する

### 3. NumPyバージョンの互換性問題

```
A module that was compiled using NumPy 1.x cannot be run in NumPy 2.0.2
```

**問題の説明**：
- PyPhiはNumPyの特定のバージョンと互換性がある
- 特にNumPy 2.0.2では問題が発生する可能性が高い

**解決方法**：
```bash
pip uninstall -y numpy
pip install numpy==1.24.3
```

### 4. メモリ使用量に関する警告

```
Memory usage warning
```

**問題の説明**：
- 大きなネットワークや複雑な計算を行う際にメモリ不足になる可能性がある
- 特に3ノード以上のシステムで発生しやすい

**解決方法**：
1. より小さなネットワークから始める
2. PyPhiの設定でメモリ使用量を調整する
3. 必要に応じてシステムのメモリを増設する

### 5. ネットワークオブジェクトの属性エラー

```
'Network' object has no attribute 'nodes'
```

**問題の説明**：
- PyPhi 1.2.0では、`Network`オブジェクトの属性やメソッドが以前のバージョンと異なる
- 特に`nodes`属性が直接アクセスできない

**解決方法**：
1. `network.size`を使用してノードの数を取得する
2. `range(network.size)`を使用してノードのインデックスを取得する
3. 必要に応じて`Subsystem`オブジェクトの`nodes`属性を使用する

**修正例**：
```python
# 誤った方法
for i, node in enumerate(network.nodes):  # エラー

# 正しい方法
for i in range(network.size):  # OK
    # ノードごとの処理
```

## ベストプラクティス

1. **段階的な実験**：
   - まず2ノードシステムで動作確認
   - 正常に動作することを確認してから3ノード以上に拡張

2. **状態の確認**：
   - 実験前に全ての可能な状態をリストアップ
   - TPMで定義された状態遷移を図で確認

3. **エラーメッセージの確認**：
   - エラーメッセージを注意深く読む
   - 具体的な問題箇所を特定してから修正を行う

4. **バージョン確認**：
   - 使用しているPyPhiのバージョンを確認
   - そのバージョンの公式ドキュメントを参照

## 参考リソース

- [PyPhi公式ドキュメント](https://pyphi.readthedocs.io)
- [PyPhiのGitHubイシュー](https://github.com/wmayner/pyphi/issues)
- [PyPhiユーザーグループ](https://groups.google.com/forum/#!forum/pyphi-users)

## トラブルシューティングの流れ

1. エラーメッセージを確認
2. このドキュメントで該当する問題を探す
3. 解決方法を試す
4. 解決しない場合は公式ドキュメントやユーザーグループを確認
5. 必要に応じて新しいイシューを報告 